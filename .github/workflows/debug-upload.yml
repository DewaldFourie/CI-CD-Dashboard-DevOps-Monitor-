name: Upload Artifact Debug Test

on: [push]

jobs:
  debug-upload:
    runs-on: ubuntu-22.04

    steps:
      - name: 🔄 Checkout repository
        uses: actions/checkout@v3

      - name: 📤 Try uploading a dummy file
        uses: actions/upload-artifact@v3
        with:
          name: dummy-artifact
          path: README.md
